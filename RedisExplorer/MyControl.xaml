<UserControl x:Class="DimensionData.RedisExplorer.MyControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 Background="{DynamicResource VsBrush.Window}"
			 Foreground="{DynamicResource VsBrush.WindowText}"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="300"
			 Name="MyToolWindow">
	<Grid>
		<DockPanel LastChildFill="True">
			<DockPanel DockPanel.Dock="Top" LastChildFill="True">
				<Button Content="Connect" Click="Connect_Click" Width="100" DockPanel.Dock="Right"/>
				<TextBox Name="urlTextBox">localhost:6379</TextBox>
			</DockPanel>
			<DataGrid Name="_dataGrid" ItemsSource="{Binding}" AutoGenerateColumns="True"></DataGrid>
		</DockPanel>
	</Grid>
</UserControl>
